# 构建系统

目前通过 xtask 实现自定义逻辑，比如打包文件系统镜像、运行在 qemu 中等。justfile 作为一个简易入口。

痛点：

- 得设置 rust-analyzer 的参数，因为 xtask 是跑在宿主机下的，而内核一般需要交叉编译

如果能够便捷地为每个项目设置 rust-analyzer 参数就好了，rust-analyzer 有个相关的 [issue](https://github.com/rust-lang/rust-analyzer/issues/13529)，可惜遥遥无期。
